<h1>Companies</h1>
<%= link_to 'Add Company', new_company_path %><br>
<ul>
<% @companies.each do |c| %>
    <li>
      <%= link_to c.name, company_path(c) %>
    </li>
  <%end%>
</ul>

<%= link_to 'All jobs', jobs_path %>
